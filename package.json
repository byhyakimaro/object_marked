{
  "name": "object_marked",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-packager . --overwrite --platform=win32 --arch=x64 --out=dist/"
  },
  "dependencies": {
    "electron": "^16.0.0",
    "electron-package": "^0.1.0"
  }
}
